/** @type {import('tailwindcss').Config} */
export default{
  darkMode: 'class', // Habilita el modo oscuro usando la clase
  //darkMode: ['selector', '[data-mode="dark"]'], // Habilita el modo oscuro usando las preferencias del sistema
  content: ["*"],
  theme: {
    extend: {},
  },
  plugins: [],

}